<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/spacing_element"
    android:paddingBottom="@dimen/spacing_element">

    <ImageView
        android:id="@+id/item_calendar_dialog_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_dot"
        style="@style/Icon"
        app:layout_constraintTop_toTopOf="@+id/item_calendar_dialog_title"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/item_calendar_dialog_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Test 1"
        style="@style/Text.Default"
        android:layout_marginStart="@dimen/spacing_page"
        app:layout_constraintStart_toEndOf="@+id/item_calendar_dialog_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
